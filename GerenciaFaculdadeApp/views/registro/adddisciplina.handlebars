{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{/each}}

<h3>Nova disciplina: </h3>
<div class="card">
    <div class="card-body">
        <form action="/registro/disciplina/novo" method="POST">
            <label for="nome">Nome: </label>
                <input type="text" id="nome" name="nome" placeholder="Nome do disciplina" class="form-control">
            <label for="codigo">Código: </label>
                <input type="text" id="codigo" name="codigo" placeholder="Código da disciplina" class="form-control">
            <label for="ch">Carga horária: </label>
                <input type="number" id="ch" name="ch" placeholder="Carga horária" min="34" class="form-control">
            <label for="professor">Selecione o professor: </label>
            <select name="professor" id="professor" class="form-control">
                {{#each professor}}
                    <option value="{{id}}">{{nome}}</option>
                {{else}}
                    <option value="0">Nenhum professor encontrado</option>
                {{/each}}
            </select>
            <label for="monitor">Selecione o monitor: </label>
            <select name="monitor" id="monitor" class="form-control">
                <option value="">Escolher um monitor</option>
                {{#each estudante}}
                    <option value="{{cpf}}">{{nome}}</option>
                {{/each}}
            </select>
            <br>
            <button type="submit" class="btn btn-success">Matricular disciplina</button>
        </form>
    </div>
</div>